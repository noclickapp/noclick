---
title: "Configuration"
---

Copy `backend/.env.example` and `frontend/.env.example` to `.env` files and set
the required database, authentication, signing, and encryption values.

For deployment topology, OAuth setup, storage, auth SMTP, Resend notifications,
and production hardening,
read the repository's `docs/self-hosting.md` guide.
