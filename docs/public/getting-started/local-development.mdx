---
title: "Local development"
---

Install Docker, the Supabase CLI, Python 3.12, Node.js 20+, and pnpm.

```bash
pip install -r requirements.txt
make local
```

The command starts a local database and authentication stack, applies the
schema, and runs the backend and frontend. Local account confirmation is
automatic.
