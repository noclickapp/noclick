{
  "$defs": {
    "SendEmailConfig": {
      "description": "Send an email to YOUR OWN account email address. The recipient is\nalways the account email of the user running the workflow \u2014 no other\nrecipients can be specified.",
      "properties": {
        "operation": {
          "const": "send",
          "default": "send",
          "title": "Email Myself",
          "type": "string",
          "ui:hidden": true,
          "x-display-name": "Email Myself",
          "x-is-trigger": false
        },
        "subject": {
          "description": "Subject line of the email.",
          "placeholder": "Daily report ready",
          "title": "Subject",
          "type": "string"
        },
        "body": {
          "description": "Body of the email. Markdown is rendered into formatted HTML automatically; ready-made HTML is used as-is. The email is sent to your own account email address only \u2014 recipients cannot be chosen.",
          "title": "Body",
          "type": "string",
          "ui:rows": 6,
          "ui:widget": "textarea"
        },
        "attachments": {
          "description": "Files to attach: workflow resource ids or URLs (max 10MB per file, 20MB total).",
          "items": {
            "type": "string"
          },
          "placeholder": "{{node.resource_id}}",
          "title": "Attachments",
          "type": "array",
          "ui:widget": "list"
        }
      },
      "required": [
        "subject",
        "body"
      ],
      "title": "SendEmailConfig",
      "type": "object"
    }
  },
  "description": "Full configuration for the send-email node (no credentials needed).",
  "properties": {
    "config": {
      "$ref": "#/$defs/SendEmailConfig",
      "description": "Node-specific configuration"
    },
    "credentials": {
      "default": null,
      "description": "Credentials for this node (optional if using environment variables)",
      "title": "Credentials",
      "type": "null"
    }
  },
  "required": [
    "config"
  ],
  "title": "Send-email Node Configuration",
  "type": "object",
  "x-edit-examples": [
    "Email me a summary of the results",
    "Send myself the daily report when the cron fires",
    "Change the subject to include the item count",
    "Email me when the scraper finds new listings"
  ],
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "x-agent-tool-provider": true
}